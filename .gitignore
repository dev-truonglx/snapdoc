node_modules
dist
dist-ssr
*.local
.DS_Store

# Rust / Tauri
src-tauri/target
src-tauri/gen
# ffmpeg sidecar (record/encoder.rs) — binary lớn, tải qua script riêng
# (scripts/fetch-ffmpeg.sh, chưa viết), không commit vào git. Bản trong máy
# dev hiện tại chỉ là copy tạm từ Homebrew để test, không dùng để phân phối.
src-tauri/binaries/

# Claude Code local settings (personal)
.claude/settings.local.json

# Release artifacts (local staging, never commit)
release_dist/
dist-win/

# Signing keys — keep this folder out of git entirely, including the
# encrypted .enc backups. Back them up to a password manager or private
# cloud storage instead (see scripts/backup-keys.sh / restore-keys.sh).
secrets/
